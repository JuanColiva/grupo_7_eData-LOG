<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include("../partials/head.ejs")  %>
        <link rel="stylesheet" href="/css/login.css">
    </head>
    <body>
        <header>
            <%- include("../partials/header.ejs")  %> 
        </header>
        <main class="main-login">
            <section class="login-box">
                <h2>Login</h2>
                <form action="/users/access" method="post" id="formulario">
                    <div id="formulario1">
                        <input type="email" 
                           placeholder="email"
                           name="email"
                           value="<%= locals.errores && !locals.errores.email && locals.data ? locals.data.email : null %>"
                           class="class-barra"
                           id="email-id">
                           <p class="class-barra-error" id="legenda-error">El email tiene solo puede ser letras, numeros y debes poner el "@" y ".com"</p>
                           <% if (locals.errores && locals.errores.email) { %>
                            <p class="feedback">
                                <%= locals.errores.email.msg  %> 
                            </p>
                            <% } %>
                    </div>
                    <div id="formulario2">
                        <input type="password"
                           placeholder="contraseña"
                           name="password"
                           value="<%= locals.errores && !locals.errores.password && locals.data ? locals.data.password : null %>"
                           class="class-barra"
                           id="password-id">
                           <p class="class-barra-error" id="legenda-error">La contraseña debe contener minimo 6 digitos</p>
                           <% console.log(locals.errores) %> 
                           <% if (locals.errores && locals.errores.password) { %>
                            <p class="feedback">
                                <%= locals.errores.password.msg  %> 
                            </p>
                            <% } %>
                    </div>
                    <label for="recordarme" class="recordarme-aqui">Recordarme aqui.
                    <input type="checkbox" id="recordarme" name="rememberMe"
                    class="caja"></label>
                    <p class="class-barra-error">Debes cliquear recordarme aqui</p>
                    <p class="debes-marcar">debes cliquear "recordarme aqui"</p>
                    <button type="submit" class="botton-form">Enviar</button>
                </form>
            </section>
            <section class="comentary-box">
                <h2>¿Primera vez aqui?</h2>
                <form>
                    <p class="texto-bienvenida">Bienvenido! estamos felices deque estés aqui. Por favor, cuentenos sobre usted.</p>
                    <input type="text" placeholder="Comentarios"
                    class="comentrios-login">
                    <button type="submit" class="botton-form">Continuar</button>
                </form>
            </section>
        </main>
        <footer class="main-footer">
            <%- include ("../partials/footer.ejs") %> 
        </footer>
        <script src="/scripts/login.js"></script>
    </body>
</html>